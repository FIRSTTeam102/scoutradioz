//-
	Finished Matches shows a list of all finished matches and their outcome.
		reports.js
		'/reports/finishedmatches'
extends ../layout
block content
	include mixins/completedMatch
	style.
		.thick-border{
			border-width: 3px!important;
			margin-top: -2px;
			border-radius: 9px;
			border-color: #dfe1e9!important;
		}
	if (matches && matches.length > 0)
		h2(class="theme-text")!=msg('reports.completedMatches')
		br 
		each match in matches
			+completedMatch(match, true)
	else
		h2(class="theme-text")
			i!=msg('reports.noMatchesYet')
